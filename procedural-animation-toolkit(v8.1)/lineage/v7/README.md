# V7 regression baseline

V7 is preserved as the contact-locked baseline immediately preceding the V8 action-expression pass. V8 intentionally reuses its lower-body and sole-contact architecture. The V7 embedded browser pages are omitted here because they duplicate the included GLB; its numeric reports and master montage are retained for regression review.
