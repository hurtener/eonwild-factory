# Agent Prompts — V7

## Reproduce the validated pack

Use the provided GLB, semantic YAML, and references. Run reference analysis, generate all V7 clips, validate the baked GLB, render side/three-quarter/overhead reviews, build the embedded browser page, and package provenance. Do not alter transforms after the contact solve. Fail closed when any anatomical, contact, endpoint, or browser contract fails.

## Review locomotion

Review at least two full cycles. Look specifically for simultaneous foot hover, terrain penetration, lateral body flight, knee/ankle branch inversion, rear overextension, mechanical head stabilization, rigid tail response, and loop displacement snapping. Cite frame/time evidence for every rejection.

## Review turning

Compare left and right turns. Confirm the head and neck visibly anticipate the future path, the chest follows, the pelvis commits later, inside/outside steps differ, and the tail counter-shapes. Reject root-yaw-only behavior even when the endpoint heading is numerically correct.

## Review actions and transitions

Run both lead-side bites, feeding, roar, alert, and idle. Then run every authored sequence in the browser. Verify exact endpoint handoff and UI state. Never hide a transition artifact with a camera cut.
