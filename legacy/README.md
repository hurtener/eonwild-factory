# Legacy procedural toolkits

The existing V5, V5.5, and V8.2 toolkit directories are immutable migration
sources. The canonical engine must not import them. A legacy entry becomes
`migrated` only after the new CLI reproduces its accepted artifact and passes
independent review.
