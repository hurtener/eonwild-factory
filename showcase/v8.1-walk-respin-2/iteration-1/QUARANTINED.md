# Rejected review media — do not use for approval

This MP4 is tied to candidate iteration 15, which was rejected because its
passive lag was actively translated instead of mechanically integrated. The
media is retained as evidence only; the next approved review will be emitted
in a separate iteration directory after the replacement gate passes.
