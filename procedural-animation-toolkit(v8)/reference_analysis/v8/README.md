# V8 reference analysis

The source video is preserved unchanged. `observed_timeline` records visible evidence; `authored_v8_interpretation` records animation-design inference. Camera cuts, zoom, and perspective prevent treating the source as calibrated motion capture.
