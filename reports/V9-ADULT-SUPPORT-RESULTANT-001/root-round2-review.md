# Root support-resultant diagnostic round 2

Final reviewed head `b4e54d48d067307d6a8c5017f4eaeba303299eb7`, source/input/test code `860233f339ff0b49cb3a1e9f525aa4b3e5d28119`.

Decision: PASS for this bounded sampled diagnostic, P0/P1=0. No motion or physical-validation approval follows.

Both R1 failures were independently re-executed against the real immutable adult V9 package. Changing only the consumed biomechanics height now rejects before output. A combined joint-origin-only plus 90/10 profile now emits only the requested model, and its two double-support resultant proxies exactly equal 0.9 times the leading proxy plus 0.1 times the trailing proxy. The retained executable-result evidence is `root-support-resultant-round2/executed-closure.json` and `joint-origin-90-10-result.json` (SHA-256 `12b9cd0f3ec655996ee97ed1425ddaaa0c1ed9f260b781434f8060b220c07a3c`). Root ran all eight new analytic/CLI tests: 8 passed in 0.97 s.

Root inspected the complete final delta and report, verified the final source/input/test hashes match the executed code, and byte-verified the retained support-geometry audit. After normalizing only renamed load-model keys, every default-model numerical output is exactly unchanged from R1. The final report now labels the tail redistribution as authored sensitivity, makes the lateral conclusion conditional on the unmeasured central-toe proxy, removes the undefined admitted-neighborhood gate, and supplies explicit package/audit arguments with the small audit retained.

The geometric envelope is an axis-aligned range diagnostic, not a convex-patch or pressure feasibility proof; the report's explicit no-convex-support-admission boundary applies. No further implementation reopen is warranted. Independent R2 remains required before integration.
