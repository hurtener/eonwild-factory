# Canonical support anchors: root round 2

Exact head: `f36b1eeb9af059f006000106624b8ae3ea143c8f`, parent `7b584f21cff3197d8afcba9fbd109ee9f29e5026`.

Decision: **CLEAN — the round-1 live-request binding P1 is closed.** This is the second and final full root review of the bounded optional-provider stage. No recipe activates it.

The consumer recomputes the source/current-scale, semantic, solver, locomotion, transition, performance/gaze, contact, coordinate and articulation binding before refinement. Ordered sole-then-toe material indices and origin payloads are checked against their retained digests. The builder rejects a solver with incompatible cadence. An explicitly requested canonical mode rejects a missing provider. Returned anchor arrays are detached copies; direct mutation of an owned array is detected before consumption.

The complete request digest includes sampling, transition and clip-domain contracts. Independently constructed compatible start/steady/stop providers still share the same sustained-event material coordinates. The smaller gait-family digest is not sufficient identity for transferring a provider to another request, or proof that an arbitrary rig is independent of the body-response sampling clock.

Root inspected the complete six-file stage and postfix. Focused provider plus phase-local legacy checks passed: **13 tests in 27.95 seconds**. A preliminary command used a nonexistent phase-local filename and ran zero tests; the corrected command used `tests/test_phase_local_solved_pose.py`.

A separate live validation probe admitted the genuine provider and rejected seven independently changed consuming inputs: performance amplitude, gaze calibration, semantic tail order, forward direction, solver joint bound, current source root translation, and contact floor. Mutating a returned anchor copy did not affect the retained provider. The committed tests separately exercise swapped sole/toe ordering, missing provider, cadence mismatch, and owned-payload corruption. Probe results were observed directly in the tool output; no separate persisted probe transcript is claimed.

No default recipe, emitted representation, engineering limit, approved source or review film changed. Canonical origins alone do not provide an arbitrary-time skin correction law, exact emitted continuity, biological validation or visual acceptance.
