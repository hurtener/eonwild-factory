# Authored material contact adapter — checkpoint 001

This checkpoint integrates the reviewed source-bound material-contact adapter onto primary base `a80150e6`. The code head before this report is `b7f3938`; Git range-diff matched all three reviewed adapter commits exactly. The affected integration gate passed 62 tests after the sparse checkout was supplied with the tracked recipe/schema trees and two exact hardlinked test fixtures.

The adapter consumes sampled Raptor Walk foot motion at the raw FBX clock of 24 fps over 31 intervals. Its support events are explicit, source-bound interpretations. It evaluates the target shared body response at the same adapted contact phase before the final leg and full-weighted-skin material solve. It preserves the requested swing-material clearance while applying the existing 1 mm canonical IK residual limit and a distinct 0.1 mm material gap.

Both narrow reviewers report P0/P1=0 at `8d789469`. Root independently evaluated 133/133 key, quarter-key, and event-neighborhood samples as AVAILABLE. That is a physical source-query gate for the sampled inventory. It is not biological validation, full serialized-curve approval, emitted-package acceptance, or native playback approval. Author rate results are sampled secants and carry no continuous or serialized-rate claim. Native rendering was still pending when this report was frozen.

The acquired Raptor animation is a user-downloaded Unity Asset Store input authorized for this project evaluation. Vendor FBX, package, mesh, and capsule bytes are excluded from Git and are not redistributed here. Their downstream use remains subject to the applicable Asset Store terms.

The original author-side `706c` dense result SHA `f7c54a…` was overwritten before head-specific retention and is explicitly unretained. Root's independent failure witness remains retained externally at SHA `bc1ced…`; the compact root closure and reviewer2 closure are copied here. Full 133-sample pose rows remain external and are hash-bound in `hash-manifest.json`.

The remaining factory work is a declared reusable input/configuration path for the adapter, followed by emitted/native and visual validation. Acquired Raptor axial/body motion is not yet injected by this checkpoint.
