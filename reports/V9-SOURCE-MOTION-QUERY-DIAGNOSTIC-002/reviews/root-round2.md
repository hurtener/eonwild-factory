# SourceMotionQuery round 2 — root

Reviewed exact postfix `270d4b2c9bdc13998cec2a3e816cfca5b4c2e362` against prior freeze `c02a17769d86841d9c9dddb66b4e0ed8e9365deb`. Scope is the consolidated diagnostic fixes and shared source identity helper only. No prior closed stage was reopened.

No outstanding P0/P1. The canonical retained-row binding rejects the demonstrated source height/gait/transition/row contradictions; contact material admission checks raw identity and mutable geometry; formal derivative authority is always explicitly unavailable; raw numerical observations carry no convergence claim. The actual contact event at 0.352 seconds now has distinct left/right contact sides. Focused source query, identity and phase-local tests: **26 passed in 7.61 s**. Independent all-interior-key/nextafter FK comparisons with nonzero airborne body response give maxima `3.426e-13 m` steady, `3.427e-13 m` start, and `4.733e-13 m` stop. Compiler default byte pins remain intact.

## Local P2 — raw one-sided probe room includes the boundary behind it

At `_raw_probe` interval selection, `other` includes the queried boundary even after the side-owned anchor has been moved approximately `8e-10 s` inside the requested side. Taking the minimum absolute distance in both directions limits the probe to that epsilon instead of the available interval in the requested direction. At the exact 0.352 s right liftoff, left raw probe is unavailable while right raw probe uses `(1.00000015212931e-10, 5.00000076064655e-11, 2.500000380323275e-11) s`. Both sides at 0.8 s are unavailable. This does not produce false derivative acceptance, but makes the new event-side diagnostic unusable or dominated by subtraction roundoff.

This is a truly local low-risk correction allowed after round two: select available event/domain room in the requested probe direction; centered probes still use both directions. Add an event-side test proving meaningful h/h2/h4 stays on the declared side, without changing any source pose, solver, or authority status. One narrow diff check is enough; no third full review.

The shared helper also has a pending local exact-cache comparison already raised to its owning jaw stage: compare `source.nodes` with reconstructed `parsed.nodes`, as with the existing cached maps/TRS. Coordinate that exact helper patch before final integration. No other full-stage review requested.
