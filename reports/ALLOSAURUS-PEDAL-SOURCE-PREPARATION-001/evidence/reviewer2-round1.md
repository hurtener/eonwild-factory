# Allosaurus weighted pedal branches reviewer2 R1 — 4eee1ba

**CHANGES_REQUIRED: P0=0, P1=2, local P2=0.**

The generic opt-in branch construction, new prepared bytes and focused tests are bounded, but two public correctness gaps block integration. Repeated primitives that share skin accessors apply partial-gain transfers more than once, so primitive enumeration changes final weights. Separately, the new public input documents bind the raw prepared GLB before geometry admission; the package used for the mechanical preflight required an external admitted copy and temporary rebindings that this commit does not ship.

The retained public diagnostic remains mechanically BLOCKED at a 4.630359 mm right loaded residual after admission. Neither finding justifies changing push-off, contact, ROM or solver limits.

Evidence: `review.json`, `shared-accessor-probe.json`, and `tests.log` in this directory.
