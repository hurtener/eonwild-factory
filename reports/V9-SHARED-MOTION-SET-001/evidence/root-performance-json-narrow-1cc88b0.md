# Narrow saved-JSON provenance closure

Reviewed clean source `1cc88b0b1669764421da1ef11cd48aa6ca9bbd08` against parent `62e4b60a399672fc68f9817aeabc6bff8c300912`.

P0=0, P1=0 in this narrow diff. The single production line serializes and reloads the reconstructed expected performance through the existing canonical JSON serializer. This gives tuple-bearing dataclass fields the same JSON-array representation as the saved plan while retaining exact key/value comparison. It changes verifier representation handling only; source motion, contacts, pose solving, thresholds and artifact identities remain unchanged.

The focused tests round-trip the actual bound animal neutral calibration and both catalog transition plans through JSON. The positive cases pass; changed pelvis yaw, changed nested jaw joint accessor, and changed/missing transition parameters still reject. Root reran the three selected tests with `PYTHONPATH=src`: 3 passed in 2.70 seconds. An initial invocation without this explicit source path failed collection against the older editable primary checkout; it was not a product regression and is retained in the receipt.

The earlier full suite remains attributed to 0330d8a; this narrow run is not a new full-suite result. The saved fast package remains generated by 62e4b60. Separate public verification and native review must retain that generator identity rather than label it as generated by this verifier fix.
