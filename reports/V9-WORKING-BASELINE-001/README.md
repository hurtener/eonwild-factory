# V9 working-baseline publication 001

Status: **V10 IS THE USER-SELECTED WORKING BASELINE; IT REMAINS UNFINISHED.** Catalog migration is `PENDING`. This record does not select a production package, change any recipe or catalog entry, establish Unity parity, approve another movement, or replace the immutable identities of older approved references.

## Adult walk working baseline

The selected review baseline is `heavy-biped.tarbosaurus-pin-552-1-adult-walk.v10` from the immutable package `out/source-cubic-adult-v10-f9ca924`. Its manifest is SHA-256 `80b15c5d7dba7aa294335c46e55e8e8776aa7d177507c3c7fb3f96628252153d`, its locked inputs are SHA-256 `a20a434116e2005a231235a046dcdffb26b70f5f6f6e2b79790db12bc76bd102`, and its root-motion and in-place CUBICSPLINE files are respectively `85f19b01805153269988865752737e368be292f8fc49d5e527dfcf5f3ec4b2a4` and `ced935c771749922cbfa3209ec76ba9a2699ec7c928797753fb94c967c31cd97`. The package reports technical `PASS`, remains a `CANDIDATE`, and has `production_approved: false`.

The current native review films are retained outside Git. V10 side and front film hashes are `d685701a6a56a0750de0b63c480191dec73655e26dcbabb7722297e5c4db9aa8` and `0ff6974bc546a8fea4093952caa49bcfdff3349d7538917f509d3ce6271eaa14`. Their matched V9 comparison films are `afed56bb4062a235811745bfe087590d6728b55e9d1cf7eaa834331ff3c2d3b4` and `d2da1acbb5f27612a69a34a34a38806f3512de471bc194581b4c3cafe5800da7`. These hashes identify the reviewed media; the media are not duplicated here.

The original acceptance receipt said `UNSELECTED`. It is retained byte-for-byte as `adult-v10-acceptance-superseded.json`. The corrected receipt, `adult-v10-working-baseline-acceptance.json`, records the user's later selection as `USER_SELECTED_WORKING_BASELINE` and keeps catalog migration `PENDING`. Remaining work includes body-weight transfer and exact continuity. This is a working baseline decision, not a Unity or production claim. Both acceptance JSONs preserve the acceptance-time `front-camera and tail visibility remain under review` note; the corrected tail evidence below supersedes that historical open item for the two investigated native witnesses.

The existing fast-walk recovery v3 uses an older body/source configuration and is not the current V10 body baseline. A shared motion-set implementation that will resolve one common animal, rig, contact, articulation, frame, and body-performance baseline across movements is in progress separately; this documentation commit does not anticipate or activate it.

## Tail and camera finding

The source-bound tail audit found all nine semantic tail joints within `0.000342 mm` of the neutral sagittal centerline. Across 149 exact half-cycle joint pairs, maximum world-lateral antisymmetry residual is `0.032164 mm` (RMS `0.022658 mm`), while tail-tip peak-to-peak travel is `1.049160 m`. Two exact native-time witnesses, frame 4 at `0.133333 s` and frame 41 at `1.366667 s`, used the corrected front axis. In both, the tail is occluded behind the body. The inherited camera has `2.231181 degrees` of azimuth relative to that corrected axis, which accounts for the apparent one-sided visibility.

This supports a camera-occlusion explanation for the reviewed front witnesses and does not justify a tail-rig or tail-motion change. It is not a full-cycle visual approval, perfect skin-symmetry proof, biological claim, or Unity result. The exact analysis script, numeric result, camera receipt, two-native-witness render receipt, and source-bound receipt are copied under `evidence/`; the still images are retained externally.

## Current start and stop technical joins

The new `heavy-biped.walk-stop.v3` CUBICSPLINE compile completed successfully at generator head `f9ca924bcc79943d2d2b7eeab28bef8763c18ec4`. Its manifest is `2cb15f92cb0b09f66b3c6cbd31d45f0ad408c4da4a181ff3fb14c482aa3d09b`. The exact stop-to-steady handoff passed in both root-motion and in-place modes under verifier head `6e3080196182688cad49a4b441753e27064824fc`; the greatest exact serialized linear-velocity residual was `0.000012206803 m/s`, below the `0.001 m/s` limit.

The latest start-to-steady report also passes both modes. Its start package was generated at `36f311e4cfc67cd73eb684a21f353d0c804ff5fb`; its exact motor-aware verifier is `60426de40279e81291b3e0affbf9bb465066b267`; the greatest exact serialized linear-velocity residual was `0.000012362932 m/s`. Both reports join against the retained `heavy-biped.walk.v3` package with manifest `03c0edb00b53abe5d68094480f2558d657f076e4099c85650ac293bb901a277c`.

Those are deliberately mixed generator/verifier identities, preserved in the copied receipts. They establish technical boundary compatibility for the exact retained packages. They do not establish a single connected-film render, perceptual acceptance of a start or stop, catalog selection, or Unity parity.

## Source-query target-offset fast path

The opt-in source-query fast path is closed at `ff85ddd59da5da8d62f769b447f8accb87800124` (tree `d51cc3b54a74e465a9aef3e7815aa8e53c37f743`). Two independent round-one reviews found one reachable subclass-ownership P1: a subclass could override the private combined-offset method and bypass the intended fallback. The exact-type guard and public-method ownership checks close that path. Both narrow diff-only reviews pass; the original reproducer and closure result are retained.

The corrected, separate-process benchmark compares the actual published parent default path with the fast path over eight identical adult samples. Parent `6e3080196182688cad49a4b441753e27064824fc` performs 16 row solves in `5.249131 s`; the fast path performs 8 in `2.904980 s`, with exact output equality. The observed speedup is `1.8069422702585032x`. The earlier approximately 4x number is discarded because its baseline forced per-side observer fallback and measured four solves per time rather than the parent's default two-solve path. This benchmark is representative runtime evidence, not a full-compile forecast.

## Evidence boundary

`evidence/SHA256SUMS` inventories every copied or newly authored receipt. Heavy GLBs, native films, still images, and large logs remain in external task storage. No engine, recipe, catalog, CI, source asset, or approved historical artifact changed in this publication.
